<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper
	namespace="jp.levtech.rookie.tutorial.repository.mybatis.TaskMapper">
	<select id="findByDate" parameterType="String" resultType="jp.levtech.rookie.tutorial.model.Todo">
  SELECT id, date, title, memo FROM todos WHERE date = #{date}
		SELECT id, date, title, memo
		FROM todos
	</select>

	<select id="findById"
		resultType="jp.levtech.rookie.tutorial.model.Todo">
		SELECT id, date, title, memo
		FROM todos
		WHERE id = #{id}
	</select>

	<insert id="register">
		INSERT INTO todos (date, title, memo)
		VALUES (#{date},
		#{title}, #{memo})
	</insert>

	<update id="update">
		UPDATE todos
		SET date = #{date},
		title = #{title},
		memo
		= #{memo}
		WHERE id = #{id}
	</update>

	<delete id="delete">
		DELETE FROM todos
		WHERE id = #{id}
	</delete>
</mapper>